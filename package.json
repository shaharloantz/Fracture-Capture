{
  "name": "Fractures-Captures",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm install && npm start",
    "client": "cd client && npm install && npm run dev"
  },
  "dependencies": {
    "express-jwt": "^8.4.1",
    "jsonwebtoken": "^9.0.2",
    "tailwindcss": "^3.4.6"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
